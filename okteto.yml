build:
  redis:
    context: .

deploy:
  compose: docker-compose.yml

dev:
  redis:
    image: redis:alpine
    command: bash
    persistentVolume:
      enabled: true
      #storageClass: standard
      size: 1Gi
    sync:
      - .:/opt/okteto/app
  