build:
  redis:
    context: .

deploy:
  compose: docker-compose.yml

dev:
  redis:
    #image: docker.io/bitnami/redis:6.2.10
    command: bash
    persistentVolume:
      enabled: true
      #storageClass: standard
      size: 1Gi
    sync:
      - .:/opt/okteto/app
  