build:
  redis:
    context: .

deploy:
  compose: docker-compose.yml

dev:
  redis:
    image: redis:6.0.17
    command: redis-server
    persistentVolume:
      enabled: true
      #storageClass: standard
      size: 1Gi
    sync:
      - .:/opt/okteto/app
  